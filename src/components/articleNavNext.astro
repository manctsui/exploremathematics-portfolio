---

---





<style is:global>
    nav {
        max-width: 34rem;
        border-top: 6px solid black;
        border-bottom: 6px solid black;
        padding: 1em 0;
    }
    nav a {
        font-size: 1.6em;
        font-weight: bold;
        text-decoration: none;
    }
    #b {
        display: inline-block;
        /* Reserve right margin for animation. Per Edge's console suggestion, transform animations do not trigger Layout redraws compared to changing padding. */
        margin-right: .4em;
    }
    nav a:hover {
        color:#56a7f3;
    }
    nav a:hover #b {
        animation: arr .7s infinite alternate;
    }
    @keyframes arr {
        to {transform: translate(.4em);}
    }
</style>