---

---

<style is:global>
    nav {
        max-width: 34rem;
        border-top: 6px solid black;
        border-bottom: 6px solid black;
        padding: 1rem 0;
        margin: 2rem 0;
    }
    nav a {
        font-size: 1.6rem;
        font-weight: bold;
        text-decoration: none;
    }
    #b {
        display: inline-block;
        /* Reserve right margin for animation. */
        margin-right: .4em;
    }
    nav a:hover {
        color:#56a7f3;
    }
    nav a:hover #b {
        animation: ar .7s infinite alternate;
    }
    @keyframes ar {
        to {transform: translate(.4em);}
    }
</style>